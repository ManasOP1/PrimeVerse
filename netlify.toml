# SPA redirect - redirect all routes to index.html EXCEPT static files
# Netlify processes redirects in order, so specific routes must come first
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

